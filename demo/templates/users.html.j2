{% extends 'base.html.j2' %}

{% block title %}
    Users Page
{% endblock %}

{% block content %}
{% include "includes/user_modals.html" %}
    <!-- Jinja2 value access from flask-->
    <table class="table table-hover table-light">
    <thead>
        <tr>
        <th scope="col">ID</th>
        <th scope="col">Name</th>
        <th scope="col">Options</th>
        </tr>
    </thead>
    <thead>
        <!-- Jinja2 logic access for loop-->
        {% for user in users %}
        <tr>
        <!-- Jinja2 access iterator values where iterator is a dict-->
        <td>{{ user.id }}</td>
        <td>{{ user.name }}</td>
        <td>
            <button class="btn btn-outline-info" data-toggle="modal" data-target="#Modal-UserInfo">More Info</button>
            <button class="btn btn-outline-danger" data-toggle="modal" data-target="#Modal-UserInfo">Remove</button>
        </td>
        </tr>
        {% endfor %}
    </thead>
    </table>
    <button class="btn btn-outline-primary">Add User</button>
{% endblock %}